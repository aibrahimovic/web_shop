<html>
	<head>
		<title>Web shop</title>
	
	</head>

	<!-- <= image_tag("carousel.jpg", :class => "cover_slika", :size => "1150x350") %> -->
	<body>
		<div id="header-wrapper">

			<div id="slider">
			  <a href="#" class="control_next">>></a>
			  <a href="#" class="control_prev"><</a>
			  <ul>
			  <% @special.each do |product| %>
			  	<% @main_image = Image.find_by(product_id: product.id, main: '1') %>
			  	<% if @main_image.nil? %>
				  	<li style="padding-top: 130px;">
				  		<span>
				  			<%= link_to product_path(product.id) do %>
				  				<%= image_tag(product.image.url(:slider)) %>
				  			<% end %>
				  		</span>
				  	</li>
			  	<% else %>
				  	<li style="padding-top: 130px;">
				  		<span>
				  			<%= link_to product_path(product.id) do %>
				  				<%= image_tag(@main_image.avatar.url(:slider)) %>
				  			<% end %>
				  		</span>
				  	</li>
			  	<% end %>
			  <% end %>
			  
			  <% @special_sale.each do |product| %>
			  	<% @main_image = Image.find_by(product_id: product.id, main: '1') %>
			  	<% if @main_image.nil? %>
				  	<li style="padding-top: 10px;">
				  		<p class="sale_tag"> <i>-<%= product.get_percent %>%</i> <p>
				  		<span>
				  			<%= link_to product_path(product.id) do %>
				  				<%= image_tag(product.image.url(:slider)) %>
				  			<% end %>
				  		</span>	
				  	</li> 			
			  	<% else %>
				  	<li style="padding-top: 10px;">
				  		<p class="sale_tag"> <i>-<%= product.get_percent %>%</i> <p>
				  		<span>
				  			<%= link_to product_path(product.id) do %>
				  				<%= image_tag(@main_image.avatar.url(:slider)) %>
				  			<% end %>
				  		</span>	
				  	</li> 	
				 <% end %>			
			  <% end %>
			  	 <!-- <= image_tag(product.image.url(:show)) %> -->
			  

			  	<!--<li style="padding-top: 80px;"><= image_tag("c1.png") %></li>
			    <li style="padding-top: 80px;"><= image_tag("c2.png") %></li>
			    <li style="padding-top: 80px;"><= image_tag("c3.png") %></li> -->

			  </ul>  
			</div>

		</div>

		<h2 id="prodaucts_on_sale" style="margin-top: 650px;"> <%= t('newest') %> </h2>


		<% counter = 0 %>
		<table id="all_products2">
		<% @all_products_on_sale.each do |product| %>
		<% @main_image = Image.find_by(product_id: product.id, main: '1') %>
			
				<% if counter%4 == 0 || counter == 0 %>
					<tr><td style="padding: 25px;">
				<% else %>
					<td style="padding: 25px;">
				<% end %>
				<%= link_to product_path(product.id) do %>
					<div data-content1="<%= product.name %>"  data-content2="<%= product.get_price %>KM"  data-content3="<%= product.get_percent %>%" data-content4="<%= product.price %>KM" class="image">
					<% if @main_image.nil? %>
						<%= image_tag(product.image.url(:small)) %><br>
					<% else %>
						<%= image_tag(@main_image.avatar.url(:small)) %><br>
					<% end %>
						<% counter += 1 %>
					</div>
				<% end %>
					<span class="percent_on_image">-<%= product.get_percent %>%</span>
					</td>
					<% if counter%4 == 0 || counter == @all_products_on_sale.length %>
						</tr>
					<% end %>
			<% end %>
			<% counter = 0 %>
		</table>

	</body>
</html>


<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
<!-- <script src="js/bootstrap.min.js"></script> -->

<%= stylesheet_link_tag    "/assets/bootstrap.min" %>
<%= javascript_include_tag "/assets/bootstrap.min" %>

<script>
jQuery(document).ready(function ($) {

  setInterval(function () {
        moveRight();
    }, 5000);



  /*$('#checkbox').change(function(){
    setInterval(function () {
        moveRight();
    }, 3000);
  });*/
  
	var slideCount = $('#slider ul li').length;
	var slideWidth = $('#slider ul li').width();
	var slideHeight = $('#slider ul li').height();
	var sliderUlWidth = slideCount * slideWidth;
	
	$('#slider').css({ width: slideWidth, height: slideHeight });
	
	$('#slider ul').css({ width: sliderUlWidth, marginLeft: - slideWidth });
	
    $('#slider ul li:last-child').prependTo('#slider ul');

    function moveLeft() {
        $('#slider ul').animate({
            left: + slideWidth
        }, 200, function () {
            $('#slider ul li:last-child').prependTo('#slider ul');
            $('#slider ul').css('left', '');
        });
    };

    function moveRight() {
        $('#slider ul').animate({
            left: - slideWidth
        }, 200, function () {
            $('#slider ul li:first-child').appendTo('#slider ul');
            $('#slider ul').css('left', '');
        });
    };

    $('a.control_prev').click(function () {
        moveLeft();
    });

    $('a.control_next').click(function () {
        moveRight();
    });

});    

</script>