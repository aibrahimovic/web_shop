
<h2>Uplatili ste <strong><%= @amount/100 %>KM</strong>!</h2>

<%= button_to "HOME", item_path(@cart, sh_address: @shipping_address, bil_address: @billing_address), :class => "btn btn-primary", :method => :delete  %>
